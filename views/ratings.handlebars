
<head>
    <meta charset="UTF-8">
    <title>ratings</title>
    <link rel="stylesheet" href="../public/css/styles.css">
</head>

<body>
    <header class="header" id="header">  
        <a href="/landing">Home</a>  
        <a href="/tools">Search</a>
        <a href="/users">Profile</a> 
        <a href="/toolsregister">Manage</a>
    </header>
    <h1>ratings</h1>
    <div id="error" class="error" hidden></div>
    {{#if hasErrors}}
        <div class="error">Error: {{error}}</div>
    {{/if}}

    {{!-- const ratingData={ratingID: req.body.ratingID,
                userID: req.body.userID,
                toolID: req.body.toolID,
                rating: req.body.rating,
                comment: req.body.comment
            }; --}}
    <form action="/ratings" id="rating-info" name="rating-info" method="POST">
        
        <label for="userID">User ID</label>
        <input type="text" id="userID" name="userID"><br>

        <label for="toolID">Tool ID</label>
        <input type="text" id="toolID" name="toolID"><br>

        <label for="rating">Rating</label>
        <input type="text" id="rating" name="rating"><br>

        <label for="comment">Comment</label>
        <input type="text" id="comment" name="comment"><br>

        <button id="submitButton" name="submitButton">Submit</button>
    </form>
    <div id="ratingregistererror" class="formerror" hidden></div>
    <script src="../public/js/client_side_validation.js"></script>
</body>
